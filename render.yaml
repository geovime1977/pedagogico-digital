services:
  - type: web
    name: pedagogico-digital
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: sh start.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
